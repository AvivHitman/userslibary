import styled from "styled-components";

const WrapButton = styled.button`
  background-color: #0be493;
  color: black;
  border-radius: 5px;
  width: 60px;
  cursor: pointer;
`;

const AddContainer = styled.div`
  width: 80%;
`;

export { WrapButton, AddContainer };
